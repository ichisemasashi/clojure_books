(ns learn-macro.exercises.ch14
  "第14章演習（判断）: チェックリストでマクロ案を判定。
  解答は answers/ch14.clj")

(def prompts
  [{:id :q1 :prompt "社内 defroute マクロ"}
   {:id :q2 :prompt "設定マップだけ作る defconfig マクロ"}
   {:id :q3 :prompt "テスト用の計測マクロ"}
   {:id :q4 :prompt "レコードアクセサ一括生成マクロ"}
   {:id :q5 :prompt "チーム独自の if 糖衣マクロ"}])
