# 核検証・要検証メモ（工程2再完了）

**実行日:** 2026-09-11  
**コマンド:** `clojure -M -m learn-macro.verify`  
**実測:** Clojure **1.12.1** / Java **26.0.2.1**（Homebrew OpenJDK）  
**本書の仮固定:** Clojure **1.12.x** + JDK **21**（実測 JDK は 26。言語挙動の確認としては 1.12.1 で充足。JDK 21 での再実行は工程4で推奨）

生ログ: [`検証実行ログ.txt`](./検証実行ログ.txt)

---

## 第1–6章・ケース A/B

| 対象 | 結果 | 要点 |
|------|------|------|
| Ch1 `and` vs 関数 | 検証済 | 関数は両引数評価、`and` は短絡 |
| Ch2 コード＝データ | 検証済 | リストを組み立てて `eval` |
| Ch3 クォート族 | 検証済 | `'` / `` ` `` / `~` / `~@` / `a#` |
| Ch4 `unless` | 検証済 | 展開は `(if (not ...) (do ...))` |
| Ch5 expand 族 | 検証済 | `macroexpand-1` / `macroexpand` / `macroexpand-all` |
| Ch6 二重評価・gensym | 検証済 | `let` 一回束縛で副作用1回。`good-time` は gensym |
| ケース A | 検証済 | `when-let-lite` 展開・実行・nil 分岐 |
| ケース B | 検証済 | 計測マクロ風 `good-time` |

---

## §4.3 要検証

| 項目 | 結果 |
|------|------|
| `&env` | **実展開箇所**では locals（例: `[a b]`）が見える。`macroexpand-1` だけだと空 `[]` になりうる |
| `foo#` | 同一構文引用内は共有。構文引用が別だと別シンボル。ネストした `` ` `` は新しい対応表 |
| `macroexpand-all` | ネストした `when`/`or` は展開。`#()` はリーダー済みの `fn*`。`let` は `let*` に落ち特殊形式として残る |
| 版 | Clojure 1.12.1 を実行で確認 |

---

## 状態更新用（調査素材§3）

上記すべて **検証済**。
